## CPA10I_DESKTOP_RUNTIME_CERTIFICATION

STATUS=PASS
HEAD_BEFORE=2549da86b19bccb013e0bad6466c6b0b9d04fac8
HEAD_AFTER=2549da86b19bccb013e0bad6466c6b0b9d04fac8
WORKTREE_BEFORE=clean; git status --short empty; git diff --name-only HEAD empty
WORKTREE_AFTER=report-only change to walkthrough.md; production/test/docs files unchanged
FILES_CHANGED=walkthrough.md only (report); production/test/docs=NONE

RUNTIME_FRESHNESS_VERIFICATION=PASS. Before Desktop certification, Contextor returned canonical_state=fresh and data_source=live_canonical_state with module_count=398. get_live_events reported activity_epoch=dae57f644c324727a2eed4b040a15a7c, revision=1213, resync_required=false, and retained RUNTIME_AUTHORITY_READY after the user restart; the active runtime lease generation was 72 and DESKTOP_CLAIM_ACQUIRE was ACTIVE for the Contextor window process. Post-run symbol checks remained canonical_state=fresh, workspace_sync=verified, provenance=live at revision 1215 for parse_source_snapshot, CacheManager.get, CacheManager.set, _process_single_file, and index_repository. No stale-source or metadata-match response occurred.

CONTEXTOR_FIRST_VERIFICATION=PASS. Deferred Contextor tools were found and used before certification. get_symbol_implementation verified the live source contract and workspace_sync=verified for parse_source_snapshot (contextor/core/source.py:85-108), CacheManager.get (contextor/core/analysis/cache_manager.py:120-158), CacheManager.set (contextor/core/analysis/cache_manager.py:160-202), _process_single_file (contextor/core/symbol_engine/indexer.py:313-665), and index_repository (contextor/core/symbol_engine/indexer.py:723-972). The current implementation is the CPA10I contract: one read_source_snapshot per file task; cache.get and cache.set receive source_bytes=source_snapshot.raw; a complete cache return occurs before parse_source_snapshot; miss/incomplete processing parses the already captured SourceSnapshot. The current live implementations were rechecked after both Desktop runs at revision 1215.

DESKTOP_RUN_1=PASS. User-triggered full Desktop repository analysis completed on the running Desktop process (pid=9084) and published canonical LIVE state. The AppData LIVE/MCO trace `C:\Users\DafoO\AppData\Roaming\Contextor\logs\contextor_runtime_20260915_140732_711_9084.jsonl` records FULL_ANALYSIS_LEASE_ACQUIRED at 2026-09-15T16:14:31.657+02:00 and FULL_ANALYSIS_END at 2026-09-15T16:15:32.365+02:00 with owner=desktop_analysis, analysis_ms=60703, total_ms=60797, and total_before_release_ms=60797. The same trace records FULL_ANALYSIS_INDEX_EVIDENCE with file_tasks=398, source_parse_calls=0, cache_get_calls=398, cache_hits=398, cache_misses=0, lineage_cache_hits=398, lineage_extract_calls=0, source_parse_sum_ms=0, and FULL_ANALYSIS_LINEAGE_MATERIALIZATION with reuse_sources=398, reresolve_sources=0, materialize_sources=0. Contextor get_live_events(repo_path, after_revision=1213) returned revision=1214, operation=publish, origin=desktop_analysis, status=PUBLISHED, file_path=null, continuity=continuous, resync_required=false. get_project_architecture immediately after returned module_count=398 and data_source=live_canonical_state.

DESKTOP_RUN_2=PASS. User-triggered second full Desktop repository analysis completed without source or cache changes and published canonical LIVE state. The same AppData trace records FULL_ANALYSIS_LEASE_ACQUIRED at 2026-09-15T16:16:42.434+02:00 and FULL_ANALYSIS_END at 2026-09-15T16:17:30.964+02:00 with owner=desktop_analysis, analysis_ms=48531, total_ms=48563, and total_before_release_ms=48563. Its FULL_ANALYSIS_INDEX_EVIDENCE again records file_tasks=398, source_parse_calls=0, cache_get_calls=398, cache_hits=398, cache_misses=0, lineage_cache_hits=398, lineage_extract_calls=0, source_parse_sum_ms=0, and lineage_extract_calls=0; FULL_ANALYSIS_LINEAGE_MATERIALIZATION records reuse_sources=398, reresolve_sources=0, materialize_sources=0. Contextor get_live_events(repo_path, after_revision=1214) returned revision=1215, operation=publish, origin=desktop_analysis, status=PUBLISHED, file_path=null, continuity=continuous, resync_required=false. get_project_architecture immediately after again returned module_count=398 and data_source=live_canonical_state.

WARM_CONTRACT_EVIDENCE=PASS. The AppData LIVE/MCO trace exposes the required Desktop counters for both runs: each processed all 398 file tasks with 398/398 cache hits, zero cache misses, source_parse_calls=0, source_parse_failures=0, lineage_extract_calls=0, and zero aggregate parse/extraction time. Both lineage materialization records show reuse_sources=398 with materialize_sources=0 and reresolve_sources=0. This is direct runtime evidence of the complete warm-cache fast path, not an inference from the prior profile. LIVE also showed no cache invalidation, resync, syntax diagnostic, or failed publication.

LIVE_EVIDENCE=PASS. Runtime activity_epoch remained dae57f644c324727a2eed4b040a15a7c. The restart sequence was present before certification: RUNTIME_AUTHORITY_START, RUNTIME_LEASE_ACTIVATION, DESKTOP_CLAIM_ACQUIRE, RUNTIME_ENDPOINT_BIND, and RUNTIME_AUTHORITY_READY. Run 1 advanced canonical revision 1213->1214 with origin=desktop_analysis/status=PUBLISHED. Run 2 advanced 1214->1215 with origin=desktop_analysis/status=PUBLISHED. After revision 1215, get_live_events returned no additional canonical events and resync_required=false. Diagnostics summary remained fresh with zero syntax, collision, and cycle attention required. The unrelated durable get_analysis_status record was an older failed MCP job (PermissionError while writing its job file) and was not used as Desktop-run evidence.

LATENCY_COMPARISON=AppData LIVE/MCO trace gives authoritative Desktop critical-path totals of 60.797 s (run 1) and 48.563 s (run 2). Relative to the earlier approximate 44 s observation, the differences are +16.797 s and +4.563 s; relative to the canonical profile warm-repeat 45.297 s, they are +15.500 s and +3.266 s. These are meaningful comparisons of the same warm-cache behavior, but not a strict 1:1 benchmark: the 45.297 s value came from the canonical contextor_profile_analysis MCP path (owner=mcp_analysis), while these values are owner=desktop_analysis on the Desktop process. The trace also shows run-1/run-2 stage timing (indexing 13250/10141 ms; live_publish 10688/9157 ms), which is retained as supporting evidence only.

EVIDENCE_LIMITATIONS=The public Desktop certification surface does not project the profiler counters/timings, but the authoritative AppData LIVE/MCO trace does. No supported Contextor MCP tool triggers a Desktop-owned run; analyze_project/profile_analysis would be an MCP-owned path and was intentionally not substituted. Native Windows UI automation was unavailable in this session, so the two runs were triggered by the user. No source/cache mutation, update_file call, ad-hoc harness, restart, or test run was performed.

MCP_SERVER_RESTART_REQUIRED=NO
DESKTOP_RUNTIME_RESTART_REQUIRED=NO (freshness verification passed after the user restart)
PROFILE_WORKER_RESTART_REQUIRED=NO
ACTUAL_DIFF=DIFFS=NONE for production/test/docs/source files. Only walkthrough.md was written as the required report; walkthrough.md is not counted as a production/test/docs change.
