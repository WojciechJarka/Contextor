# Contextor runtime dependencies.
#
# Install with:  python -m pip install -r requirements.txt
#
# This is the complete runtime set. Everything else Contextor uses
# (ast, tkinter, concurrent.futures, ...) ships with Python itself.
# Requires Python 3.10 or newer.
#
# Test tooling is a separate, optional extra:
#     python -m pip install -e ".[dev]"

# Fast JSON serialization for report output.
orjson>=3.9
# MCP server integration
mcp>=1.0.0
