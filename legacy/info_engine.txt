Raport z refaktoryzacji modułu `reporting_engine/engine.py`

Stary moduł `engine.py` (łamiący SRP) został przeniesiony tutaj, aby w razie awarii można było szybko go przywrócić.

**Zmiany w importach (gdzie i na co zmieniono):**
W pliku `contextor.core.api.facade`:
- `generate_summary_report` -> przeniesiono z `.reporting_engine.engine` do `.reporting_engine.generators`
- `generate_structure_report` -> przeniesiono z `.reporting_engine.engine` do `.reporting_engine.generators`
- `slice_report_for_layer` -> przeniesiono z `.reporting_engine.engine` do `.reporting_engine.generators`
- `save_all_reports` -> przeniesiono z `.reporting_engine.engine` do `.reporting_engine.io_manager`
- `save_layer_reports` -> przeniesiono z `.reporting_engine.engine` do `.reporting_engine.io_manager`

W pliku `contextor.core.reporting_engine.__init__`:
- Zaimportowano odpowiednie metody z `generators` i `io_manager` zamiast z `engine`.

Plik `contextor/core/reporting_engine/engine.py` został usunięty z drzewa rdzenia.
