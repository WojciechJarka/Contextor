{
  "version": "1.0.0",
  "tool": "get_module_context",
  "purpose": [
    "[OPTIMIZED] Retrieve a compressed context pill for one module."
  ],
  "parameters": [
    "repo_path (string, required): canonical repository root.",
    "module_name (string, optional, default \"\"): module identifier (accepts module ID e.g. '259/1', dotted module name, or file path).",
    "max_items (integer or null, default 30): maximum number of dependency items returned in full mode (compact mode caps evidence at 3 items); pass null for all items.",
    "compact (boolean, default true): controls compact evidence shaping (bounded evidence) vs verbose items.",
    "fields (array of strings or null, default null): optional field projection list; null returns the full response.",
    "module (string or null, optional, default null): alias for module_name."
  ],
  "behavior": [
    "Resolution order:\n1. Exact active module ID via active module registry.\n2. Canonical LIVE state lookup by dotted module name or file path.\n3. Artifact-input diagnosis / redirect to get_artifact_blast_radius.\n4. Bounded fuzzy module suggestions (score >= 0.75, max 5) from active module registry on textual module not-found.\n5. Legacy not-found string fallback when query is not found or is a nonexistent module ID.",
    "Dependency & degree metrics definitions:\n  - ``metrics.fan_in``: Direct inbound module degree from hard dependency/import edges only.\n  - ``dependencies_inbound_who_calls_me``: All direct inbound dependencies (both hard AST imports and soft reachability edges)."
  ],
  "freshness": [
    "Full-report metrics are combined with the canonical LIVE dependency graph.\nA file added through update_file is therefore immediately queryable, even\nbefore another global report refreshes expensive metrics. Deferred metrics\nare labelled explicitly instead of hiding the module.",
    "Every response includes a ``state_freshness`` envelope describing canonical state and workspace sync:\n- ``canonical_state``: Internal health of canonical facts (fresh, stale, deferred).\n- ``workspace_sync``: Disk-vs-canonical comparison (verified: sha256 confirmed; metadata_match: mtime+size match without sha256; out_of_sync: disk differs; unverified: cannot resolve target file).\n- ``canonical_revision``: Integer revision of the state answering the query (hydration-bound, not daemon reachability).\n- ``provenance``: live or snapshot.\n- ``families``: Per-family freshness flags (module, graph, topology, artifact_consumption, cycles, collisions).\n- ``advisory_warning``: Non-null if out_of_sync, metadata_match, or last job was interrupted/failed."
  ],
  "errors": [
    "Available dependency collections expose ``total`` and ``truncated``. The default ``compact=True`` response includes bounded ``evidence`` mapping module names to dependency types, with at most 3 entries per dependency collection. Explicit ``max_items`` values below 3 reduce that evidence limit; ``max_items=None`` does not remove the compact evidence cap. Use ``compact=False`` for full edge ``items`` and ``compact=False, max_items=None`` for lossless complete dependency collections. ``truncated`` means that fewer elements are present in the returned collection than its ``total``. Truncated collections include ``expand={\"compact\": false, \"max_items\": null}`` describing the lossless expansion call. ``compact`` shapes the response before ``fields`` projects it. Allowed values are ``module``, ``metrics``, ``metrics_source``, ``degree_metrics_source``, ``dependency_data_source``, ``dependencies_inbound_who_calls_me``, and ``dependencies_outbound_who_i_call``. Invalid projections return a structured error with the current allowlist."
  ],
  "usage_notes": [
    "LLM use: call immediately before editing a known module. Trust LIVE\ninbound/outbound edges for current structure; treat metrics marked\ndeferred as unavailable until the next full analysis."
  ],
  "examples": []
}
