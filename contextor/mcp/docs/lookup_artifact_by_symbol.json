{
  "version": "1.0.0",
  "tool": "lookup_artifact_by_symbol",
  "purpose": [
    "[OPTIMIZED] Find a canonical artifact in LIVE state by symbol name, module-qualified identity, or artifact ID."
  ],
  "parameters": [
    "repo_path (string, required): canonical repository root.",
    "symbol_name (string, optional, default \"\"): symbol name or identity to look up (accepts active artifact ID, full canonical identity 'module::symbol', or leaf symbol name).",
    "limit (integer or null, default 20): maximum number of matching artifacts returned; pass null for all.",
    "evidence_limit (integer or null, default 20): maximum number of nested evidence items per candidate.",
    "compact (boolean, default true): when true, returns bounded evidence items with total and truncated flags; set to false for full items list.",
    "fields (array of strings or null, default null): optional projection list; null returns full response.",
    "symbol (string or null, optional, default null): alias for symbol_name."
  ],
  "behavior": [
    "Searches the active artifact registry for exact and fuzzy symbol matches.\nReturns a bounded collection of resolved artifact entries with definer, consumers, and signature facts."
  ],
  "freshness": [
    "The response includes a ``state_freshness`` envelope. Because this tool resolves across the full artifact registry (repo-wide scope), ``workspace_sync`` is always ``unverified`` — the tool cannot cheaply verify all matched source files without a repo scan.",
    "- ``canonical_state``: Internal state freshness flag (fresh/stale).",
    "- ``workspace_sync``: ``unverified`` (repo-wide search does not perform per-file disk stat/hash without a scoped target).",
    "- ``canonical_revision``: Exact revision of the canonical state used to answer this query.",
    "- ``provenance``: live or snapshot.",
    "- ``advisory_warning``: Non-null if the last analysis job was interrupted/failed."
  ],
  "errors": [],
  "usage_notes": [
    "LLM use: use to resolve a symbol to its canonical identity before calling get_artifact_blast_radius.\nThe state_freshness.workspace_sync='unverified' is expected here; call a scoped tool for exact file verification."
  ],
  "examples": []
}
